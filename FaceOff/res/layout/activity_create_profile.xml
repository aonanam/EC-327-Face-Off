<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".CreateProfile" 
    android:background="@drawable/profile_creation_background">

    <TextView
        android:id="@+id/create_profile"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="6dp"
        android:text="Create Profile"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:textSize="50dp"
        android:textColor="#ffffff" />

    <ImageButton
        android:id="@+id/baseface_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="110dp"
        android:background="@android:color/transparent"
        android:onClick="activity_base_face_tutorial"
        android:src="@drawable/create_base_face_button" />

    <EditText
        android:id="@+id/name_entry"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/baseface_button"
        android:layout_alignLeft="@+id/create_profile"
        android:layout_marginBottom="39dp"
        android:ems="10"
        android:hint="Enter a New Profile Name..."
        android:imeOptions="actionDone"
        android:inputType="textCapWords" >

        <requestFocus />
    </EditText>

</RelativeLayout>
